This is meant as a way to easily setup a frame of reference for save data to start from.

Ideally when we load up a game, this save data will be what maintains everything.


We'll be using JsonSerialize for this because it's faster.





We would have a mono component in the scene acting as a singleton. 
we'd do a find in the scene for all objects that use some saving and loading. an interface.

The scriptableobject should only be used as a mock. the mock would be assigned to the component. by default, we'd load from disk. 
There will be editor functionality for this mock save data as well so that we can configure some data before entering playmode. this override would never be written to.

if we assign the override, then the object used by  